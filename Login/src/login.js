export default function errorForm($buttonLogin,$userLogin,$passwordLogin,$errorMessage) {
  const d = document;

  d.addEventListener("click", e => {
    if (e.target.matches($buttonLogin)) {
      d.getElementById("")
    }
  })

}

